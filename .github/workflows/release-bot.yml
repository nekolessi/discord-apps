name: Release Bot

on:
  workflow_dispatch:
    inputs:
      bot:
        description: "Bot folder under bots/"
        required: true
        default: "neko-starter-bot"
        type: choice
        options:
          - neko-starter-bot
          - neko-gif-caption-bot
          - neko-8ball-bot
      version:
        description: "Version label (example: v1.0.0)"
        required: true
        default: "v1.0.0"
        type: string
      publish_release:
        description: "Publish a GitHub Release"
        required: true
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  package:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve bot input
        id: bot
        env:
          BOT_INPUT: ${{ github.event.inputs.bot }}
        run: |
          set -euo pipefail

          case "$BOT_INPUT" in
            neko-starter-bot|neko-gif-caption-bot|neko-8ball-bot)
              echo "bot=$BOT_INPUT" >> "$GITHUB_OUTPUT"
              ;;
            *)
              echo "::error title=Invalid bot::Unsupported bot selection."
              exit 1
              ;;
          esac

      - name: Validate version input
        id: version
        env:
          VERSION: ${{ github.event.inputs.version }}
        run: |
          set -euo pipefail

          if ! [[ "$VERSION" =~ ^v[0-9]+\.[0-9]+\.[0-9]+([-.][0-9A-Za-z.]+)?$ ]]; then
            echo "::error title=Invalid version::Use semver label like v1.0.0"
            exit 1
          fi

          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Detect bot type
        id: detect
        env:
          BOT: ${{ steps.bot.outputs.bot }}
        run: |
          set -euo pipefail
          if [ -f "bots/${BOT}/package.json" ]; then
            echo "type=node" >> "$GITHUB_OUTPUT"
          elif [ -f "bots/${BOT}/requirements.txt" ]; then
            echo "type=python" >> "$GITHUB_OUTPUT"
          else
            echo "::error title=Invalid bot::No package.json or requirements.txt in bots/${BOT}"
            exit 1
          fi

      - name: Setup pnpm
        if: ${{ steps.detect.outputs.type == 'node' }}
        uses: pnpm/action-setup@v4

      - name: Setup Node
        if: ${{ steps.detect.outputs.type == 'node' }}
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install Node dependencies
        if: ${{ steps.detect.outputs.type == 'node' }}
        run: pnpm install --frozen-lockfile

      - name: Build selected Node bot
        if: ${{ steps.detect.outputs.type == 'node' }}
        env:
          BOT: ${{ steps.bot.outputs.bot }}
        run: pnpm --filter "./bots/${BOT}" build

      - name: Setup Python
        if: ${{ steps.detect.outputs.type == 'python' }}
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: bots/${{ steps.bot.outputs.bot }}/requirements.txt

      - name: Validate selected Python bot
        if: ${{ steps.detect.outputs.type == 'python' }}
        env:
          BOT: ${{ steps.bot.outputs.bot }}
        run: |
          set -euo pipefail
          pip install -r "bots/${BOT}/requirements.txt"
          python -m compileall -q "bots/${BOT}"

      - name: Create release zip
        id: package
        env:
          BOT: ${{ steps.bot.outputs.bot }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          set -euo pipefail
          mkdir -p dist
          ARTIFACT="${BOT}-${VERSION}.zip"
          zip -r "dist/${ARTIFACT}" \
            "bots/${BOT}" \
            "LICENSE" \
            -x "*/.venv/*" "*/__pycache__/*" "*/.cache/*" "*/node_modules/*"
          echo "artifact=${ARTIFACT}" >> "$GITHUB_OUTPUT"

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ steps.package.outputs.artifact }}
          path: dist/${{ steps.package.outputs.artifact }}

      - name: Publish GitHub Release
        if: ${{ github.event.inputs.publish_release == 'true' }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.bot.outputs.bot }}-${{ steps.version.outputs.version }}
          name: ${{ steps.bot.outputs.bot }} ${{ steps.version.outputs.version }}
          files: dist/${{ steps.package.outputs.artifact }}
